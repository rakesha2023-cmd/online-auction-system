apiVersion: v1
kind: Service
metadata:
  name: online-auction-system-service
spec:
  type: NodePort
  selector:
    app: online-auction-system
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
      nodePort: 30081
